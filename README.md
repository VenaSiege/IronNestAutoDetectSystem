# 铁巢自动化侦察系统

一个基于 `Python + Tkinter` 的铁巢重炮自动化目标探测系统，可根据用户提供数据自动计算目标方位角和距离。
可搭配仰角计算器使用。

## 运行方式

确保本机可用 `Python 3`，然后在项目目录执行：

```bash
python main.py
```

## 功能概览

- 20 x 10 大地图，列为 `A-T`，行为 `1-10`
- 单个大格支持进入 10 x 10 小格视图
- 支持放置铁巢、观测点、参考点、敌军目标点
- 支持手动新建、打开、保存、另存为
- 支持在大地图摘要显示主要标识信息
