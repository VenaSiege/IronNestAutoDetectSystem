# 游戏地图 GUI 小工具

一个基于 `Python + Tkinter` 的本地桌面地图小工具，提供大地图浏览、小格放大、标识点管理，以及 JSON 保存与加载功能。

## 运行方式

确保本机可用 `Python 3`，然后在项目目录执行：

```bash
python main.py
```

## 功能概览

- 20 x 10 大地图，列为 `A-T`，行为 `1-10`
- 单个大格可进入 10 x 10 小格放大视图
- 支持放置铁巢、观测点、参考点、敌军目标点
- 支持手动新建、打开、保存、另存为
